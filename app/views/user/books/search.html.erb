<%= form_tag(user_books_search_path, method: :get) do %>
  <%= text_field_tag :title, @title %>
  <%= button_tag type: "submit" %>
<% end %>

<% if @books %>
  <% @books.each do |book| %>
    <%= image_tag book.book_image_url %>
    <%= book.title %>
    <%= book.author %>
  <% end %>
<% end %>